# Real GAEB samples

Drop real-world GAEB files here (e.g. the 535-position `.x83` tender sample).
Parser tests pick up every `*.x81`–`*.x86` in this directory automatically and
skip cleanly when none are present, so CI stays green without the samples.

Files in this directory are for local testing only — check licensing before
committing tender documents to the repository.
